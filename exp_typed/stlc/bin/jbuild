(jbuild_version 1)

(executable
  ((name repl)
   (modules (repl))
   (libraries (stlc))
   (flags (-w +A-4 -warn-error +A-4))))

(executable
  ((name test)
   (modules (test test_term test_repl))
   (libraries (oUnit stlc))
   (flags (-w +A-4 -warn-error +A-4))))

(ocamllex lexer)

(rule
 (targets parser.ml parser.mli)
 (deps
  (:< parser.mly))
 (action
  (chdir
   .
   (run ocamlyacc --strict -v %{<}))))

(library
 (name type_operators_imp)
 (libraries type_operators_exp utilities)
 (flags -w +A-4 -warn-error +A-4))
